# Control plane: the HTTP endpoint that receives normalized inbound events and runs the
# inbound loop. Stateless; all durable state is in the mailbox.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dear-agent-control-plane
  labels:
    app.kubernetes.io/name: dear-agent
    app.kubernetes.io/component: control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dear-agent
      app.kubernetes.io/component: control-plane
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dear-agent
        app.kubernetes.io/component: control-plane
    spec:
      serviceAccountName: dear-agent-control-plane
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: control-plane
          image: quay.io/dear-agent/dear-agent:latest
          imagePullPolicy: IfNotPresent
          command: ["dear-agent-http"]
          args: []
          envFrom:
            - configMapRef:
                name: dear-agent-config
          env:
            - name: FASTMAIL_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: dear-agent-jmap
                  key: FASTMAIL_API_TOKEN
                  optional: true
            - name: DEAR_AGENT_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: dear-agent-postgres
                  key: DATABASE_URL
                  optional: true
            - name: DEAR_AGENT_INBOUND_SECRET
              valueFrom:
                secretKeyRef:
                  name: dear-agent-inbound
                  key: DEAR_AGENT_INBOUND_SECRET
                  optional: true
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: dear-agent-control-plane
spec:
  selector:
    app.kubernetes.io/name: dear-agent
    app.kubernetes.io/component: control-plane
  ports:
    - name: http
      port: 80
      targetPort: http
