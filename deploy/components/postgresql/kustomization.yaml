apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# Opt-in PostgreSQL for the durable queue (ADR 0005). Overlays include it under
# `components:`; a local dev environment can use scripts/dev-postgres.sh instead.
resources:
  - statefulset.yaml
  - service.yaml
  - networkpolicy.yaml
