apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# Egress allowlist for runner Jobs (see networkpolicy.yaml). Included by the overlays; the
# default blocks link-local/metadata and allows the model/Git, and you tighten it to CIDRs.
resources:
  - networkpolicy.yaml
