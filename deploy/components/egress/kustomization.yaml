apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# Opt-in egress allowlist for runner Jobs (see networkpolicy.yaml). Add it under
# `components:` in an overlay once you have filled in the model/Git CIDRs.
resources:
  - networkpolicy.yaml
